# Web Framework 
fastapi==0.115.0
uvicorn[standard]==0.30.6          # ASGI server, [standard] includes websocket + uvloop

# Database 
sqlalchemy==2.0.35                 # ORM
psycopg2-binary==2.9.9             # PostgreSQL driver (binary = no compile needed)
alembic==1.13.3                    # migrations

# Validation & Settings 
pydantic==2.9.2                    # data validation (FastAPI uses this internally too)
pydantic-settings==2.5.2           # BaseSettings for reading .env into typed config

# Auth 
python-jose[cryptography]==3.3.0   # JWT token creation + verification
python-multipart==0.0.12           # required for FastAPI to parse form data (login form)

# Utilities 
python-dotenv==1.0.1               # load .env file locally (outside Docker)